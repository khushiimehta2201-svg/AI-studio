fastapi==0.116.1
uvicorn[standard]==0.35.0
jinja2==3.1.6
python-multipart==0.0.20
PyMuPDF==1.26.3
Pillow==11.3.0
imageio-ffmpeg==0.6.0
requests==2.32.4
python-dotenv==1.1.1
opencv-python==4.10.0.84
numpy==1.26.4
pytesseract==0.3.13
# System dependency: pytesseract needs the tesseract-ocr binary installed
# separately (e.g. `apt install tesseract-ocr` on Linux, or the Windows
# installer from https://github.com/UB-Mannheim/tesseract/wiki) -- pip
# only installs the Python wrapper, not the OCR engine itself.

# Optional local Kokoro TTS demo backend (Apache 2.0)
kokoro==0.9.4
soundfile==0.13.1
